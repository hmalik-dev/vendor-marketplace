# Schema review

Review of the 28 tables in `packages/db/src/schema/*.ts`, checked against the newest snapshot `packages/db/drizzle/meta/` and against the writers and readers in `apps/api/src`, `apps/web/src` and `packages/db/src/scripts`.

Every table ends `.enableRLS()`: row-level security is **enabled, not forced**. The table owner (the role the API connects as) bypasses it, and so does any role with `BYPASSRLS` (`neondb_owner` has it on `dev` and `staging`). Any other role sees nothing until a policy grants access. Enabling RLS is defence in depth. Authorisation is still done in the API.

Classes:

- **user-owned**: a row belongs to one customer or vendor.
- **public-read**: vendor profiles, reviews, categories and the rows that hang off a published profile.
- **operator-only**: admin actions, alerts, cases, settings, applications and invites.
- **system**: lookup and job tables.

Abbreviations: PK primary key, FK foreign key, NN not null, UQ unique index, partial = has a WHERE clause. FK actions are cascade, restrict or set null. Writers and readers are the DAO/service that holds the query; the web app reaches every table through the API.

| Table                     | Class         | Primary key               | Foreign keys (ON DELETE)                                                                                                  | Nullability, unique and check constraints                                                                                                                                         | Indexes                                                                                      | Written by                                           | Read by                                                        |
| ------------------------- | ------------- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | ---------------------------------------------------- | -------------------------------------------------------------- |
| `users`                   | user-owned    | id uuid                   | none                                                                                                                      | NN auth_user_id, email, role, names, counters, is_banned; UQ auth_user_id; UQ email partial (deleted_at null)                                                                     | role; deleted_at partial; banned partial                                                     | users.dao, admin.dao, reviews.dao                    | auth-sync, admin, cases, bookings daos                         |
| `vendor_profiles`         | public-read   | id uuid                   | user_id -> users (cascade)                                                                                                | NN user_id, business_name, slug; UQ user_id, slug; check stripe_onboarded requires stripe_account_id                                                                              | published+deleted; city+state partial                                                        | vendors.dao, admin.dao, users.dao, reviews.dao       | vendor-search, vendor-profile, admin, customers daos           |
| `categories`              | public-read   | id uuid                   | none                                                                                                                      | NN name, slug; UQ name, slug                                                                                                                                                      | none beyond UQ                                                                               | admin-categories.dao                                 | categories.routes, vendor-search, admin daos                   |
| `vendor_categories`       | public-read   | vendor_id + category_id   | vendor_id -> vendor_profiles (cascade); category_id -> categories (cascade)                                               | both NN                                                                                                                                                                           | category_id                                                                                  | vendors.dao                                          | vendor-search, vendor-profile, dashboard daos                  |
| `tags`                    | system        | id uuid                   | none                                                                                                                      | NN name, slug, category; UQ slug; UQ category+name                                                                                                                                | category+display_order partial                                                               | admin.dao                                            | vendor-search, admin.dao, web refine bar                       |
| `vendor_tags`             | public-read   | vendor_id + tag_id        | vendor_id -> vendor_profiles (cascade); tag_id -> tags (cascade)                                                          | both NN                                                                                                                                                                           | tag_id                                                                                       | tags.dao, admin.dao                                  | vendor-profile, vendors, tags daos                             |
| `tag_suggestions`         | user-owned    | id uuid                   | vendor_id -> users (cascade); resolved_tag_id -> tags (set null)                                                          | NN vendor_id, suggested_name, category, status; UQ category+lower(name) partial (pending)                                                                                         | status+created_at; vendor_id                                                                 | tags.dao, admin.dao                                  | tags.dao, admin.dao                                            |
| `portfolio_items`         | public-read   | id uuid                   | vendor_id -> vendor_profiles (cascade)                                                                                    | NN vendor_id, image_url                                                                                                                                                           | vendor_id+display_order                                                                      | portfolio.dao, cover-from-portfolio                  | vendor-profile, portfolio, upload-sweep, admin daos            |
| `service_packages`        | public-read   | id uuid                   | vendor_id -> vendor_profiles (cascade)                                                                                    | NN vendor_id, name, description, price_cents, inclusions; no check on price                                                                                                       | vendor_id+is_active                                                                          | packages.dao                                         | vendor-profile, booking-requests, payments, admin daos         |
| `availability`            | public-read   | id uuid                   | vendor_id -> vendor_profiles (cascade)                                                                                    | NN vendor_id, date, status; UQ vendor_id+date                                                                                                                                     | UQ only                                                                                      | availability.dao, booking-requests.dao, payments.dao | availability, booking-requests, admin-detail daos              |
| `booking_requests`        | user-owned    | id uuid                   | customer_id -> users (cascade); vendor_id -> vendor_profiles (cascade); package_id -> service_packages (set null)         | NN customer_id, vendor_id, event_date, status; UQ live request per customer+vendor+date+package (partial), and without package (partial); UQ vendor+date where accepted (partial) | customer+status; vendor+status; expires_at partial                                           | booking-requests.dao, payments.dao, admin.dao        | booking-requests, dashboard, customers, messaging daos         |
| `bookings`                | user-owned    | id uuid                   | request_id -> booking_requests (restrict); customer_id -> users (restrict); vendor_id -> vendor_profiles (restrict)       | NN amounts, payout_model, status, counters; UQ request_id; UQ vendor+date where confirmed (partial)                                                                               | customer_id; vendor_id; stripe_payment_intent_id; payout_due partial; payout_failing partial | payments.dao, payouts.dao, refunds.dao               | booking-requests, cases, customers, admin daos                 |
| `conversations`           | user-owned    | id uuid                   | customer_id -> users (cascade); vendor_id -> vendor_profiles (cascade); booking_request_id -> booking_requests (set null) | NN customer_id, vendor_id; UQ booking_request_id; UQ customer+vendor partial (open)                                                                                               | customer+last_message_at; vendor+last_message_at                                             | messaging.dao, booking-requests.dao                  | messaging, reports, data-rights daos                           |
| `messages`                | user-owned    | id uuid                   | conversation_id -> conversations (cascade); sender_id -> users (cascade)                                                  | NN conversation_id, sender_id, content                                                                                                                                            | conversation_id+created_at                                                                   | messaging.dao                                        | messaging, data-rights daos                                    |
| `reviews`                 | public-read   | id uuid                   | booking_id -> bookings (cascade); reviewer_id -> users (cascade); vendor_id -> vendor_profiles (cascade)                  | NN all keys, type, rating, content, is_public; check rating 1 to 5; UQ booking_id+reviewer_id                                                                                     | booking_id; vendor_id+created_at partial (public)                                            | reviews.dao                                          | reviews, vendor-profile, customers, admin daos                 |
| `review_tombstones`       | system        | booking_id + reviewer_id  | booking_id -> bookings (cascade); reviewer_id -> users (cascade)                                                          | both NN                                                                                                                                                                           | none beyond PK                                                                               | reviews.dao                                          | reviews.dao                                                    |
| `legal_acceptances`       | user-owned    | id uuid                   | vendor_id -> vendor_profiles (cascade); accepted_by_user_id -> users (cascade)                                            | vendor_id nullable; NN document, version, sha256, method, accepted_by_*; UQ user+document+version                                                                                 | vendor+document; user+document                                                               | legal-acceptance.dao                                 | legal-acceptance, payments, booking-requests, data-rights daos |
| `notifications`           | user-owned    | id uuid                   | user_id -> users (cascade)                                                                                                | NN user_id, type, title; body, data nullable                                                                                                                                      | user_id+read_at+created_at                                                                   | messaging.dao, reviews.dao                           | messaging, notification-email, data-rights daos                |
| `email_deliveries`        | system        | id uuid                   | user_id -> users (cascade); notification_id has no FK by design                                                           | NN notification_id, user_id, recipient_email, type, sent_at, outcome; UQ provider_message_id partial                                                                              | user_id+sent_at; related entity partial; UQ provider id                                      | email-delivery.dao                                   | email-delivery, operator-alerts daos                           |
| `admin_actions`           | operator-only | id uuid                   | actor_id -> users (cascade)                                                                                               | NN actor_id, action, subject_type, subject_id, detail                                                                                                                             | created_at; actor+created_at; subject_id+created_at; action+created_at                       | admin.dao                                            | admin.dao                                                      |
| `support_cases`           | operator-only | id uuid                   | sender_user_id -> users (set null); booking_id -> bookings (set null); resolved_by -> users (set null)                    | NN reference, origin, status, message; UQ reference; UQ stripe_dispute_id                                                                                                         | status+created_at; booking_id; status+subject                                                | cases.dao                                            | cases, operator-alerts daos                                    |
| `operator_alerts`         | operator-only | id uuid                   | none                                                                                                                      | NN kind, subject_id, outcome, sent_at; UQ subject_id partial (digest date)                                                                                                        | kind+subject_id+sent_at                                                                      | operator-alerts.dao                                  | operator-alerts.dao, stripe.routes                             |
| `stripe_webhook_failures` | operator-only | id uuid                   | none                                                                                                                      | NN failure, failed_at                                                                                                                                                             | failure+failed_at                                                                            | operator-alerts.dao                                  | operator-alerts.dao                                            |
| `platform_settings`       | operator-only | id uuid (singleton)       | updated_by -> users (set null)                                                                                            | NN pause flags; check singleton id; check max_booking_cents null or positive                                                                                                      | none                                                                                         | platform-settings.dao                                | platform-settings.dao                                          |
| `vendor_applications`     | operator-only | id uuid                   | none                                                                                                                      | NN email, business_name, category, city, message, status; UQ email; check email lowercase                                                                                         | created_at                                                                                   | vendor-invites.dao                                   | vendor-invites.dao                                             |
| `vendor_invites`          | operator-only | id uuid                   | invited_by -> users (set null)                                                                                            | NN email; UQ email; check email lowercase                                                                                                                                         | UQ only                                                                                      | vendor-invites.dao                                   | vendor-invites.dao                                             |
| `us_cities`               | system        | name + state              | none                                                                                                                      | NN population, search_name                                                                                                                                                        | search_name prefix                                                                           | seed.ts (reference seed)                             | places.dao                                                     |
| `refund_attempts`         | system        | payment_intent_id + scope | none                                                                                                                      | NN failed_attempts                                                                                                                                                                | none beyond PK                                                                               | refunds.dao                                          | refunds.dao                                                    |

## Differences from what the code assumes

- **Package price range is enforced only in code.** `service_packages.price_cents` is `integer NOT NULL` with no check (`packages/db/src/schema/service-packages.ts:33`); the range lives in `priceCentsSchema` (`packages/shared/src/schemas/index.ts:265-273`, used at 688 and 713). Any other writer (admin, script) can store zero or negative. Verdict: file against packages/db (a check needs a generated migration, not a one-line schema change).
- **Typical guest count ordering is enforced only in code.** `users.typical_guest_count_min` and `_max` are independent nullable integers (`packages/db/src/schema/users.ts:51-52`) while the Zod refinement requires min <= max (`packages/shared/src/schemas/index.ts:424-431`). Verdict: file against packages/db.
- **Foreign keys with no supporting index**, so deleting the parent row scans the child table: `booking_requests.package_id` (`bookings.ts:43`, set null), `messages.sender_id` (`messaging.ts:70`, cascade), `reviews.reviewer_id` (`reviews.ts:30`; the unique index leads with booking_id, `reviews.ts:47`), `review_tombstones.reviewer_id` (`reviews.ts:83`; PK leads with booking_id, `reviews.ts:88`), `support_cases.sender_user_id` (`support-cases.ts:72`), `support_cases.resolved_by` (`support-cases.ts:156`), `vendor_invites.invited_by` (`vendor-invites.ts:46`), `platform_settings.updated_by` (`platform-settings.ts:34`), `tag_suggestions.resolved_tag_id` (`tags.ts:97`). Verdict: file against packages/db (each needs an index and a migration; the user-delete path makes `messages.sender_id` and `reviews.reviewer_id` the ones that matter).

## Checking RLS on a live database

```sql
select c.relname from pg_class c join pg_namespace n on n.oid = c.relnamespace
 where n.nspname = 'public' and c.relkind = 'r' and not c.relrowsecurity;
```

Zero rows means every table is covered. `rolbypassrls` on the connecting role decides whether a later `FORCE` can bind it at all (VEN-504: `true` for `neondb_owner` on `dev` and `staging`).
