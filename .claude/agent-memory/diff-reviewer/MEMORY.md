- [Unpinned safety constants](review-checklist-unpinned-safety-constants.md) — flip the literal and re-run the caller's suite; for a derived constant, mutate its _inputs_, not it
- [Pseudo-element hit areas](review-checklist-pseudo-element-hit-areas.md) — `before:size-11` on a glyph button is clipped by `app-pane`'s overflow; measure with elementFromPoint, not className
- [Schema edits that 500 a screen](review-checklist-widened-write-schema-vs-response-schemas.md) — a relaxed column mismatches a read model, and [a z.date() has no wire coercion](review-checklist-zdate-without-a-wire-coercion.md); grep every response schema. And [a constant-answer branch that gains an upstream await](review-checklist-constant-branch-gains-an-upstream-await.md) sends the only unblocking screen to the error boundary
- [Read-time overlay vs its sibling write](review-checklist-read-time-overlay-vs-sibling-write.md) — the PUT returns the same list and the client stores it; and lazy expiry means the overlay's status predicate lies
- [onConflict target vs other unique indexes](review-checklist-onconflict-target-vs-other-unique-indexes.md) — an "idempotent" upsert absorbs one constraint; and [a new partial unique index reds old suites via fixture-helper defaults and deploy via rows already written](review-checklist-new-unique-index-vs-fixture-helper-defaults.md) — grep every helper default, then look for the dedupe-migration precedent (0008, 0023)
- [A setWhere-refused write still emails](review-checklist-setwhere-refused-write-still-emails.md) — `?? existing?.id` fires the one-shot send on the rows the upsert discarded, the row lock ends before the background counter is written, and the fake gateway's key dedupe keeps "sends once" green with the gate deleted
- [Derived src flips at commit](review-checklist-derived-src-flips-after-commit.md) — "derive, don't cache" swaps the rendered value the instant success fires; check both sources and what validates them
- [EventSource reconnect goes untested](review-checklist-eventsource-reconnect-untested.md) — cache its single-use ticket; nothing drives `onerror`. And [an abort listener after an await](review-checklist-abort-listener-after-await.md) — count the awaits before `addEventListener('abort')`
- [CI selector tables assume specs partition by route](review-checklist-selector-table-assumes-specs-partition.md) — `find` shadows a nested prefix, two specs share a route, and a runtime route sweep can't be keyed on one; invert the map from every `goto(`
- [Tailwind v4 transition lists](review-checklist-tailwind-v4-transition-lists.md) — `transform` is not `scale`/`translate`/`rotate`/`filter`, and leaving `box-shadow` in keeps the ring ramping; compile + probe in Chromium
- [Hand-rolled focus trap vs portals](review-checklist-handrolled-focus-trap-vs-portals.md) — Radix popover content lives at `<body>`, so `panel.contains(activeElement)` yanks focus on every Tab and Escape closes both layers
- [Source-grep guards that cannot fail](review-checklist-source-grep-substring-collisions.md) — `toContain('gap-2')` passes on `gap-2.5` or the docstring beside it ([regex truncation](review-checklist-source-guard-regex-truncation.md)); and a [subtree scan](review-checklist-subtree-scan-vs-same-file-subcomponent.md) misses the helper beside it. Count matches, then mutate
- [Frame box model is per-bundle](review-checklist-design-frames-are-content-box.md) — the screens .dc.html is content-box, every delta bundle is border-box; grep `box-sizing` in the file the diff cites. Read [skeleton geometry](review-checklist-skeleton-geometry-from-the-wrong-frame.md) off the component, not the loading frame; an undefined ramp step compiles to nothing; and [a ring offset re-grounded in the parent's colour](review-checklist-ring-offset-same-as-ground.md) deletes its only 3:1 edge — scan pixels outward
- [srcSet x-descriptors resize an unsized img](review-checklist-srcset-x-descriptor-shrinks-unsized-images.md) — intrinsic width is divided by the density, and a 2x candidate the optimizer cannot upscale renders _smaller_ than the 1x; sort call sites by which axes CSS pins
- [Seeded rows vs the production writer](review-checklist-seeded-rows-vs-the-production-writer.md) — diff the seed's `values` against the service that owns that insert; a column the seed nulls is a null branch the UI renders for ever. And [an app write to a mirrored users column is reverted by the auth reconcile](review-checklist-mirrored-column-rewritten-by-reconcile.md) in staging/prod only
- [Dirty tree vs the reviewed commit](review-checklist-dirty-tree-vs-reviewed-commit.md) — `git status` before probing; the lane may already have fixed the defect you were asked to confirm
- [Controlled index drops the selection seed](review-checklist-controlled-index-drops-the-selection-seed.md) — hoisting an active index turns `useState(seed)` into `useState(0)`; open with a non-zero committed value, press Enter
- [PGlite cannot prove a race](review-checklist-pglite-serialises-transactions.md) — a `Promise.all` lock test passes with the lock deleted, and a [recompute in a SET clause](review-checklist-recompute-in-set-clause-races.md) still races; prove it on Docker Postgres with two connections; and [a plpgsql trigger without `SET search_path`](review-checklist-plpgsql-trigger-without-search-path.md) is flipped by a shadow `evil.users` (TRUNCATE never fires a row trigger). A **new precondition** neuters every `expect([200,400]).toContain(...)` race: grep the `*.contention.test.ts` fixtures for what the gate now needs. And [an e2e branch gated on a row the spec itself plants](review-checklist-e2e-branch-gated-on-a-row-the-spec-plants.md) is inert from run 2 — re-derive run 2's entry state
- [Status filter vs webhook idempotency](review-checklist-status-filter-vs-webhook-idempotency.md) — narrowing a shared finder blinds `recordSuccessfulPayment`'s "already recorded?" read; a redelivery then 409s for three days
- [Single-URL validator vs list-shaped rows](review-checklist-single-url-validator-vs-list-shape.md) — `new URL(value)` once is blind to a comma-separated `WEB_URL`; read each row's shape regex, then feed a two-entry value
- [.dc.html tag balance](review-checklist-dc-html-tag-balance.md) — a re-cut block's surplus `</div>` evicts a frame from its `.sc`; string-sliced frame tests can't see it. Diff div counts, then diff `closest('.sc')` in Chromium
- [Path guard vs URL normalization](review-checklist-guard-parses-raw-path-url-normalises.md) — `split('/')` ownership checks miss `a/<victim>/./c`; run the candidate through the write schema, then `new URL()`, and compare
- [Viewer anchor vs the read behind it](review-checklist-viewer-anchor-vs-the-read-behind-it.md) — re-anchoring a surface on the viewer's clock without widening the server read behind it; an absent row in a sparse table is a claim, not a gap
- [LIMIT added under a post-fetch filter](review-checklist-limit-added-under-a-post-fetch-filter.md) — paging a read whose service filters afterwards returns `[]` on page 1; probe with pageSize=2, not the default. And [paging a once-whole list](review-checklist-paging-a-rendered-list.md): random-uuid tiebreak, page-1 refresh evicts the open row, other loaders narrow
- [Derivation gated on field presence](review-checklist-derivation-gated-on-field-presence.md) — a new `xStatusOf` member behind `field !== undefined` fires only where that DAO projection selects the column; open every projection, and check the branch's position
- [Layout gate checked at its own route](review-checklist-layout-gate-checked-at-its-own-route.md) — a rule narrowed to `/^\/admin$/` passes both directions of the guard while every child falls through; mutate rules by narrowing, not only dropping. And [a redirect keyed on a derived state](review-checklist-redirect-keyed-on-a-derived-state.md) fires for every other writer that produces it — grep the blocker name in the service
- [Async options vs a controlled active index](review-checklist-async-options-vs-controlled-active-index.md) — dropping `filter` makes Enter commit row 0 of the _previous_ query; hang the second request and press Enter
- [Relaxation clears half a paired filter](review-checklist-relaxation-clears-half-a-paired-filter.md) — `Anywhere` patches `city` only, leaves `state=IL`, then empties the escape list; apply the patch and re-run toSearchQuery. And [a pair gated on either half but written only on both](review-checklist-pair-gated-on-either-written-on-both.md) passes a lone half it discards
- [isDirty gate locks the escape hatch](review-checklist-isdirty-gate-locks-the-escape-hatch.md) — clear a `min(2)` field: the form is dirty AND unsavable, so the switch gated on `!isDirty` never re-enables. Presence is not usability
- [Ceiling counted over a stale lookup](review-checklist-ceiling-counted-over-a-stale-lookup.md) — fixing the add side leaves `categoryOf(id)` undercounting ids the selection outran; render with an id absent from `allTags` and fill the category
- [focus()-opener is dead when already focused](review-checklist-focus-opener-dead-when-already-focused.md) — a new affordance in `combobox-field` opens via `inputRef.focus()`; after Escape or a keyboard commit that fires nothing. Drive open→Escape→click
- [Universal boundary vs a viewer countdown](review-checklist-universal-boundary-vs-viewer-countdown.md) — `isUniversallyPastDate` keeps yesterday, so `days <= 0 → 'today'` labels a past date today; probe yesterday's row and diff the sibling surface
- [Migration idx vs origin/main](review-checklist-migration-idx-vs-origin-main.md) — a sibling lane may already own `00NN`; the snapshot is the same path in both and snapshot-parity reads the last one
- [Gate redirect in a shared client helper](review-checklist-gate-redirect-in-shared-client-helper.md) — a push inside `useApi` fires from the always-mounted header bell, bouncing the reader off `/terms` and `/support`; list the chrome, then the routes it renders on
- [Repo-wide source guards fire on new files](review-checklist-repo-wide-source-guards-fire-on-new-files.md) — a new `app/` route is red in the parity ledger, the apostrophe scan and the truncate scan; run the whole web suite, the diff cannot show you. And a [forbidden-token scan matches its own docstring and allow-list reasons](review-checklist-repo-wide-token-guard-matches-itself.md) — rerun its predicate in node, never eyeball it
- [Build-time output keyed on a passThrough env](review-checklist-build-time-output-keyed-on-passthrough-env.md) — WEB_URL is outside turbo's cache key, but `NEXT_PUBLIC_*` is inferred into web#build anyway, and the root `.env` next.config loads is hashed by nothing
- [Overlay rule changed in one composer](review-checklist-overlay-rule-changed-in-one-composer.md) — `composeLocks` says "exactly as `readCalendar` overlays"; grep the changed function's name, a comment citing it is a second implementation
- [Env stub vs the deployed required set](review-checklist-env-stub-vs-deployed-required-set.md) — `loadEnv()` fills the rows a test's stub forgot from the untracked `.env`; recompute requiredness from the registry, then grep ci.yml; an "expects unset" test is red where ci.yml's top `env:` sets it
- [Half-fragmented guard needles](review-checklist-half-fragmented-guard-needles.md) — "built from fragments" is a claim; `['legacy','<tok>'].join('_')` leaves the word whole. Run the guard's grep and count against the allow-list; and a deleted `.gitleaks.toml` entry reds CI over history
- [Turbo inputs cover only some outside reads](review-checklist-turbo-inputs-cover-only-some-outside-reads.md) — `shared`'s guard reads the whole tree and root `.env.example`; grep the package for `../../..` reads, then diff `--dry=json` hashes. An mtime-only `touch` never busts a content hash
- [Guard moved to onRequest shadows route hooks](review-checklist-guard-moved-to-onrequest-shadows-route-hooks.md) — rate-limit appends after the guard, so refused callers stop being counted; two error branches share `400 VALIDATION_ERROR`; and [a reused alert kind+subjectId is swallowed by the 6h dedupe](review-checklist-reused-alert-kind-and-subject-dedupes.md)
- [Captured-boundary ReDoS fix narrows matches](review-checklist-captured-boundary-regex-narrows-matches.md) — `(^|[^class])` loses `%3DeyJ…` and `a@x.com%2Cb@y.com`; diff base vs new via tsx, time every regex in the chain
- [Caller claims vs diff --stat](review-checklist-caller-claims-vs-diff-stat.md) — a fix the brief or decision log describes may be missing from the diff; also `has-[:focus-visible]` does not fire on a self-focused button
