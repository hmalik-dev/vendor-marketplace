/**
 * The vendor door — `design/delta-vendors/FOR-VENDORS-PROMPT.md`.
 *
 * The nav, the footer and both landing-band controls lead here, so the path is
 * written once. Every link to it opens the page from the top.
 */
export const FOR_VENDORS_PATH = '/for-vendors';

/**
 * The id of the page's payouts section, the target of its own in-page
 * `See how payouts work ↓` jump. Nothing outside the page links to it.
 */
export const FOR_VENDORS_PAYOUTS_ANCHOR = 'payouts';
